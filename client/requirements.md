## Packages
recharts | Data visualization for crowd statistics
framer-motion | Smooth animations for page transitions and UI elements
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes efficiently

## Notes
Authentication uses session-based auth with credentials: "include"
QR Scanning is primarily simulated via UI for reliability during development/demo
Live crowd updates require polling interval on dashboard
